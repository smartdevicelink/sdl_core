The bin/ directory is for binary dependencies of the project. Currently, it
should contain Closure Compiler and YUI Compressor to minimize web files during
iOS builds. More information is here: native/ios/doc/HOWTO.BUILD.txt 
