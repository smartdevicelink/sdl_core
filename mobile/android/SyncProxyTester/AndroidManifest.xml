<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ford.syncV4.android"
    android:versionCode="1"
    android:versionName="1.1 (CW28)" >

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- Required to pair Bluetooth devices -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required to check if WiFi is enabled -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Required to make the device stay awake while doing XML tests -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    
    <application
        android:debuggable="true"
        android:icon="@drawable/fiesta"
        android:label="@string/tester_app_name" >

        <!-- Required to support the USB Accessory mode with API Level 10 -->
        <uses-library android:name="com.android.future.usb.accessory" />

        <activity
            android:name=".activity.SyncProxyTester"
            android:label="@string/tester_app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:label="Locked" android:screenOrientation="portrait" android:name="com.ford.syncV4.android.policies.PoliciesTesterActivity"></activity>
        
        <service android:name=".service.ProxyService" >
        </service>

        <receiver android:name=".receivers.SyncReceiver" >
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED" />
                <action android:name="android.bluetooth.device.action.ACL_DISCONNECTED" />
                <action android:name="android.media.AUDIO_BECOMING_NOISY" />
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".activity.SoftButtonsListActivity"
            android:label="@string/title_activity_soft_buttons_list" android:theme="@android:style/Theme.Dialog">
        </activity>

        <activity
            android:name=".activity.SoftButtonEditActivity"
            android:label="@string/title_activity_soft_button_edit"
            android:theme="@android:style/Theme.Dialog" >
        </activity>
        
        <activity android:label="@string/app_name" android:name="com.lamerman.FileDialog">
        </activity>
    </application>

    <!-- Version 10 is minimum to support the add-on library for the USB Accessory mode -->
    <uses-sdk android:minSdkVersion="10" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" >
    </uses-permission>

    <!-- Required to use the USB Accessory mode -->
    <uses-feature android:name="android.hardware.usb.accessory" />

</manifest>
